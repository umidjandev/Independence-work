<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.independencework">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/sss"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sss"
        android:supportsRtl="true"
        android:theme="@style/Theme.IndependenceWork">
        <activity
            android:name=".presentention.activity.FinalActivity"
            android:exported="false" />
        <activity
            android:name=".presentention.activity.SecondActivity"
            android:exported="false" />
        <activity
            android:name=".presentention.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>